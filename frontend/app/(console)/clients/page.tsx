"use client";

import { PagePlaceholder } from "@/components/dashboard/PagePlaceholder";

export default function ClientsPage() {
  return (
    <PagePlaceholder
      title="Clients"
      blurb="Connected client devices across the current Organization's venues."
    />
  );
}
