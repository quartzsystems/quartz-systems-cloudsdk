"use client";

import { PagePlaceholder } from "@/components/dashboard/PagePlaceholder";

export default function NetworksPage() {
  return (
    <PagePlaceholder
      title="Networks"
      blurb="SSIDs and network configuration provisioned to the current Organization."
    />
  );
}
