"use client";

import { PagePlaceholder } from "@/components/dashboard/PagePlaceholder";

export default function VenuesPage() {
  return (
    <PagePlaceholder
      title="Venues"
      blurb="Venues within the current Organization, sourced from the CloudSDK provisioning service."
    />
  );
}
